/* The dual appraisal threshold is a protocol parameter (Q-005). It is bound
   in the module until the parameters object arrives with the marketplace
   phase; the default keeps the demo path single appraisal. */
export const DUAL_APPRAISAL_THRESHOLD = Symbol('DualAppraisalThreshold');
