services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: depawn
      POSTGRES_PASSWORD: depawn
      POSTGRES_DB: depawn
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
